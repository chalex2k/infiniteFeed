import 'package:flutter/material.dart';

class UICommon {
  static const appTitle = "Flutter redux";

  static const likeEnabledIcon = Icons.favorite;
  static const likeDisabledIcon = Icons.favorite_outline;
  static const likeColorIcon = Colors.red;
  static const likeSizeIcon = 50.0;
}
